import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kayıt ol — Careofy",
  description: "Ücretsiz Careofy hesabı oluştur.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
